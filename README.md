# NetworkPacket-LivepcapSave
A full-stack network packet analyzer built with React + Vite and Python Flask + WebSocket. Captures live packets from your Wi-Fi interface, visualizes traffic in real time, and simultaneously writes a .pcap file — so any network attack or suspicious activity is permanently recorded and can be opened directly in Wireshark for deep forensic analysis.
